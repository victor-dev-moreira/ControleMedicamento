@model List<ListarFornecedorViewModel>

@{
    Layout = "_Layout";
    ViewBag.Titulo = "Listagem De Fornecedores";
}

<p>
    <a class="btn btn-primary" asp-action="Cadastrar">
        <i class="bi bi-patch-plus"></i>
        Cadastrar Fornecedor
    </a>
</p>

@if (Model.Count == 0)
{
    <div class="alert alert-warning col-6" role="alert">
        Não há fornecedores cadastrados!
    </div>
}
else
{
    <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 row-cols-xl-4 g-3 mt-4">

        @foreach (ListarFornecedorViewModel f in Model)
        {
            <div class="col">
                <div class="card h-100">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-center mb-2">
                            <h5>@f.Nome</h5>
                            <span class="badge text-bg-secondary">@f.Id</span>
                        </div>

                        <dl class="row mb-2">
                            <dt class="col-sm-4">Telefone</dt>
                            <dd class="col-sm-8">@f.Telefone</dd>

                            <dt class="col-sm-4">CNPJ</dt>
                            <dd class="col-sm-8">@f.Cnpj</dd>
                        </dl>

                        <div class="d-flex gap-2 justify-content-end mt-auto">
                            <a asp-action="Editar" asp-route-id="@f.Id" class="btn btn-outline-primary" title="Editar">
                                <i class="bi bi-pencil"></i>
                            </a>

                            <a asp-action="Excluir" asp-route-id="@f.Id" class="btn btn-outline-danger" title="Excluir">
                                <i class="bi bi-trash3"></i>
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
}
